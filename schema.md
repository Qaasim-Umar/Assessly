| table_name             | columns                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| ---------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| admin_profiles         | id uuid NOT NULL, username text NOT NULL, school_code text NOT NULL, created_at timestamp with time zone DEFAULT now()                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| course_subject_combos  | id bigint NOT NULL, exam_type USER-DEFINED NOT NULL, course_name text NOT NULL, subjects ARRAY NOT NULL                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| exam_configs           | id bigint NOT NULL, exam_type USER-DEFINED NOT NULL, display_name text NOT NULL, total_questions integer NOT NULL, time_limit_minutes integer NOT NULL, instructions text, created_at timestamp with time zone NOT NULL DEFAULT now()                                                                                                                                                                                                                                                                                                                       |
| exams                  | id uuid NOT NULL DEFAULT gen_random_uuid(), title text NOT NULL, subject text NOT NULL, class_level text NOT NULL DEFAULT 'SS1'::text, type text NOT NULL DEFAULT 'Test'::text, duration integer, difficulty text NOT NULL DEFAULT 'Mixed'::text, question_type text NOT NULL DEFAULT 'MCQ'::text, status text NOT NULL DEFAULT 'Draft'::text, question_count integer NOT NULL DEFAULT 0, created_at timestamp with time zone DEFAULT now(), show_results boolean DEFAULT true, school_code text, is_general boolean NOT NULL DEFAULT false                 |
| mock_exam_answers      | id bigint NOT NULL, session_id bigint NOT NULL, question_id uuid NOT NULL, subject text NOT NULL, selected_option USER-DEFINED, is_correct boolean, is_flagged boolean NOT NULL DEFAULT false, answered_at timestamp with time zone                                                                                                                                                                                                                                                                                                                         |
| mock_exam_sessions     | id bigint NOT NULL, user_id uuid NOT NULL, exam_type USER-DEFINED NOT NULL, course_name text, subjects ARRAY NOT NULL, total_questions integer NOT NULL, time_limit_seconds integer NOT NULL, total_score integer, subject_scores jsonb, was_auto_submitted boolean NOT NULL DEFAULT false, status USER-DEFINED NOT NULL DEFAULT 'active'::session_status, started_at timestamp with time zone NOT NULL DEFAULT now(), completed_at timestamp with time zone                                                                                                |
| past_question_answers  | id bigint NOT NULL, session_id bigint NOT NULL, question_id uuid NOT NULL, selected_option USER-DEFINED, is_correct boolean, answered_at timestamp with time zone NOT NULL DEFAULT now()                                                                                                                                                                                                                                                                                                                                                                    |
| past_question_sessions | id bigint NOT NULL, user_id uuid NOT NULL, exam_type USER-DEFINED NOT NULL, year smallint NOT NULL, subject text NOT NULL, mode USER-DEFINED NOT NULL, time_limit_seconds integer, score integer, total_questions integer NOT NULL, status USER-DEFINED NOT NULL DEFAULT 'active'::session_status, started_at timestamp with time zone NOT NULL DEFAULT now(), completed_at timestamp with time zone                                                                                                                                                        |
| practice_answers       | id bigint NOT NULL, session_id bigint NOT NULL, question_id uuid NOT NULL, selected_option USER-DEFINED, is_correct boolean, time_taken_seconds integer, answered_at timestamp with time zone NOT NULL DEFAULT now()                                                                                                                                                                                                                                                                                                                                        |
| practice_sessions      | id bigint NOT NULL, user_id uuid NOT NULL, subject text NOT NULL, topic text, total_questions integer NOT NULL, difficulty USER-DEFINED, timer_enabled boolean NOT NULL DEFAULT false, time_limit_seconds integer, show_explanation_instantly boolean NOT NULL DEFAULT false, score integer, status USER-DEFINED NOT NULL DEFAULT 'active'::session_status, started_at timestamp with time zone NOT NULL DEFAULT now(), completed_at timestamp with time zone                                                                                               |
| profiles               | id uuid NOT NULL, full_name text NOT NULL, created_at timestamp with time zone DEFAULT now(), school_code text                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| questions              | id uuid NOT NULL DEFAULT gen_random_uuid(), exam_id uuid, text text NOT NULL, type text NOT NULL DEFAULT 'MCQ'::text, topic text DEFAULT 'General'::text, difficulty text NOT NULL DEFAULT 'Medium'::text, options jsonb, order_index integer DEFAULT 0, correct_answer integer, image_url text, subject text, exam_type USER-DEFINED, year smallint, explanation text, hint text, is_active boolean NOT NULL DEFAULT true, school_code text, paper_order integer, university text                                                                          |
| submissions            | id uuid NOT NULL DEFAULT gen_random_uuid(), exam_id uuid NOT NULL, student_name text DEFAULT 'Student'::text, answers jsonb NOT NULL, score integer NOT NULL, total integer NOT NULL, percentage numeric NOT NULL, submitted_at timestamp with time zone DEFAULT now(), theory_answers jsonb DEFAULT '{}'::jsonb, theory_status text DEFAULT 'none'::text, theory_marks jsonb DEFAULT '{}'::jsonb, mcq_score integer DEFAULT 0, final_score integer DEFAULT 0, final_percentage integer DEFAULT 0                                                           |
| survival_answers       | id bigint NOT NULL, session_id bigint NOT NULL, question_id uuid NOT NULL, selected_option USER-DEFINED, is_correct boolean NOT NULL, difficulty_at_time USER-DEFINED NOT NULL, streak_at_time integer NOT NULL DEFAULT 0, answered_at timestamp with time zone NOT NULL DEFAULT now()                                                                                                                                                                                                                                                                      |
| survival_leaderboard   | session_id bigint, user_id uuid, display_name text, subject text, score integer, highest_streak integer, questions_answered integer, ended_at timestamp with time zone, rank_in_subject bigint, rank_overall bigint                                                                                                                                                                                                                                                                                                                                         |
| survival_sessions      | id bigint NOT NULL, user_id uuid NOT NULL, subject text, lives_start integer NOT NULL DEFAULT 3, lives_remaining integer NOT NULL DEFAULT 3, current_difficulty USER-DEFINED NOT NULL DEFAULT 'easy'::difficulty_level, score integer NOT NULL DEFAULT 0, highest_streak integer NOT NULL DEFAULT 0, current_streak integer NOT NULL DEFAULT 0, questions_answered integer NOT NULL DEFAULT 0, status USER-DEFINED NOT NULL DEFAULT 'active'::session_status, started_at timestamp with time zone NOT NULL DEFAULT now(), ended_at timestamp with time zone |
| user_topic_stats       | id bigint NOT NULL, user_id uuid NOT NULL, subject text NOT NULL, topic text NOT NULL, total_attempts integer NOT NULL DEFAULT 0, correct_attempts integer NOT NULL DEFAULT 0, last_attempted_at timestamp with time zone                                                                                                                                                                                                                                                                                                                                   |